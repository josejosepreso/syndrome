spring:
  application:
    name: application

  datasource:
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    url: ${DATABASE_URL}

  jpa:
    hibernate:
      ddl-auto: validate
    show:
      sql: true

server:
  port: 8081

default:
  username: BAUTISTA01
